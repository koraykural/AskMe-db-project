import { QuestionFormUtils } from './question-form-utils';

describe('QuestionFormUtils', () => {
  it('should create an instance', () => {
    expect(new QuestionFormUtils()).toBeTruthy();
  });
});
