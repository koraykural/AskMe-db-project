import { CustomTimeagoFormatter } from './custom-timeago-formatter';

describe('CustomTimeagoFormatter', () => {
  it('should create an instance', () => {
    expect(new CustomTimeagoFormatter()).toBeTruthy();
  });
});
